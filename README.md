# Boat Ticketing — Offline First Laravel Foundation
P1-01/P1-02 starter. Local Laravel + local MySQL/MariaDB is the operational layer; cloud sync is asynchronous.
Create a fresh Laravel app, copy these files into it, configure `.env`, then run:
php artisan migrate
php artisan db:seed
